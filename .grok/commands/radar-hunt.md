---
description: Hunt pending binaries across domains and install today’s world-events file
---

Load the `radar` skill. Run a **hunt**: at least three domains, type A/B only, write `data/local/inbox/x_posts_YYYY-MM-DD.json`, convert with `scripts/radar-grok.ps1 convert`. Do not hero-card type X gap-ups. Research only.
